---
const currentPath = Astro.url.pathname;

const navItems = [
  { href: '/', label: 'Home' },
  { href: '/category/tech', label: 'Tech' },
  { href: '/category/travel', label: 'Travel' },
];
---

<header class="sticky top-0 z-40 w-full backdrop-blur-md bg-white/90 border-b border-slate-200">
  <div class="max-w-5xl mx-auto px-4 sm:px-6 h-16 flex items-center justify-between">
    <!-- ロゴ / サイト名 -->
    <a href="/" class="flex items-center gap-2 group">
      <span class="text-2xl">🧭</span>
      <span class="font-bold text-xl text-slate-900 group-hover:text-brand-600 transition-colors">
        Takeda Log
      </span>
      <span class="text-xs font-semibold px-2 py-0.5 rounded-full bg-slate-100 text-slate-600 border border-slate-200">
        Tech & Travel
      </span>
    </a>

    <!-- ナビゲーション -->
    <nav class="flex items-center gap-1 sm:gap-2">
      {navItems.map((item) => {
        const isActive = item.href === '/' ? currentPath === '/' : currentPath.startsWith(item.href);
        return (
          <a
            href={item.href}
            class={`px-3 py-1.5 rounded-lg text-sm font-medium transition-colors ${
              isActive
                ? 'bg-brand-50 text-brand-700 font-semibold'
                : 'text-slate-600 hover:text-slate-900 hover:bg-slate-100'
            }`}
          >
            {item.label}
          </a>
        );
      })}
    </nav>
  </div>
</header>
